---
name: Publish Branch
on:
  push:
    branches:
      - '**'
concurrency:
  group: publish-${{ github.sha }}
  cancel-in-progress: true
jobs:
  publish:
    uses: ./.github/workflows/publish.yml
    permissions:
      contents: read
      packages: write
